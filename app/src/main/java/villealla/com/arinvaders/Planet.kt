package villealla.com.arinvaders

/*
* The planet probably won't have too many stats, but for the sake of consistency,
* I added it as a class as well.
* @author Ville Lohkovuori
* */

class Planet(hitPoints: Long = 7000000000) {

    // we could have a PlanetManager to do this, to mimic the pattern with ships,
    // but since only one planet is needed, I think that'd be overkill
    fun renderInArSpace() {


    }

} // end class